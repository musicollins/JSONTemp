@page
@model IndexModel
@{
    ViewData["Title"] = "Home page";
}

<div class="container">
    <div class="card-deck">
        @foreach (var movie in Model.Movies)
        {
            <h1>@movie.Title</h1>
        }
    </div>
</div>
